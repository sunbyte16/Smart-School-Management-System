# Root Environment Variables Example
# Copy this file to create your own .env files in backend and frontend directories

# Backend Environment Variables (backend/.env)
PORT=5000
MONGODB_URI=your_mongodb_connection_string_here
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters

# AI Provider - Choose ONE
# Option 1: Google Gemini (Free tier available)
GEMINI_API_KEY=your_gemini_api_key_here

# Option 2: OpenAI (Requires credits)
# OPENAI_API_KEY=your_openai_api_key_here

# Option 3: Use Mock AI for testing (no API key needed)
USE_MOCK_AI=false

NODE_ENV=development

# Frontend Environment Variables (if needed)
# VITE_API_URL=http://localhost:5000
